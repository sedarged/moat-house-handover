# Access Backend Artifacts (Stage 1)

This folder contains the design-time backend artifacts for the split Access backend.

## Structure
- `schema/` table and index definitions
- `seed/` required initial lookup/config data
- `setup/` deployment and split-db guidance

## Important
Attachments and generated reports are **not stored as DB binaries**. Only metadata paths are saved in Access.
